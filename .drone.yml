--- 
clone: 
  depth: 1
kind: pipeline
name: azrimPipeline
steps: 
  - 
    environment: 
      TG_TOKEN: 
        from_secret: TOKEN
      CHANNEL_ID:
        from_secret: CHATID
    commands: 
    commands:
      - curl -LO "https://repo.archlinuxcn.org/x86_64/glibc-linux4-2.35-2-x86_64.pkg.tar.zst" && bsdtar -C / -xvf "glibc-linux4-2.35-2-x86_64.pkg.tar.zst"
      - pacman -Sy && pacman -S git bc inetutils python3 cpio zip jdk8-openjdk --noconfirm
      - "bash build_script.sh"
    image: archlinux:base-devel
    name: Build
